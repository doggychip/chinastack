services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      - BATTLE_BOTS=3

  frontend:
    build: ./frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
